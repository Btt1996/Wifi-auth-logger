# Minimal hostapd logging settings (example)
interface=wlan0
driver=nl80211
ssid=MyNetwork
hw_mode=g
channel=6
ieee80211n=1
wpa=2
wpa_key_mgmt=WPA-PSK
wpa_passphrase=REPLACE_WITH_YOUR_KEY

# Logging: send hostapd messages to syslog
logger_syslog=-1
logger_stdout=-1
logger_syslog_level=2
